{
  "name": "serverless-graphql-lambda-simple",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "apollo-server-lambda": "^1.3.2",
    "graphql": "^0.13.1"
  },
  "scripts": {
    "deploy": "serverless deploy",
    "destroy": "serverless remove",
    "invoke:provider": "serverless invoke -f query -p ./query-invoke.json",
    "invoke:local": "serverless invoke local -f query -p ./query-invoke.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^1.26.0"
  }
}
